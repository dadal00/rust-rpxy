name: Docker Build

on:
  push:
    branches:
      - "**"

permissions:
  packages: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate image tags
        run: |
          REMOTE="ghcr.io/${{ github.repository_owner }}/reverse_proxy"
          echo "REMOTE=$REMOTE" >> "$GITHUB_ENV"

          SHA="${{ github.sha }}"
          echo "REMOTE_SHA=${SHA:0:7}" >> "$GITHUB_ENV"

          RAW_MSG="${{ github.event.head_commit.message }}"
          SANITIZED_MSG=$(printf '%s' "$RAW_MSG" | tr '[:upper:]' '[:lower:]' | tr ' ' '-' | tr -cd 'a-z0-9._-')
          echo "REMOTE_MSG=${SANITIZED_MSG:-no-msg}" >> "$GITHUB_ENV"

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/Dockerfile
          platforms: "linux/arm64"
          push: ${{ github.ref == 'refs/heads/base' }}
          cache-to: type=gha
          cache-from: type=gha
          tags: ${{ env.REMOTE }},${{ env.REMOTE_SHA }},${{ env.REMOTE_MSG }}
